export async function verifyExtractionAccuracy(workflowExecutionId: string, resultFilePath: string) {
  // load the json result file
  // send it to the LLM along with the original document and see if accuracy is > 80
}
